use crate::ast::*;

grammar;

pub ConstantExp: Constant = {
    r"[+-]?\d" => Constant::Integer((<>).parse().unwrap()),
    r"[+-]?\d\.\d" => Constant::Float((<>).parse().unwrap())
};

pub Input: Constant = {
    ConstantExp
};

